import { FilterCommercePipe } from './filter-commerce.pipe';

describe('FilterCommercePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommercePipe();
    expect(pipe).toBeTruthy();
  });
});
